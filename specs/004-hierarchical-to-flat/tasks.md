
# Задачи: Преобразование иерархических Excel-отчётов в плоские данные

**Входные данные**: Дизайн-документы из `/specs/004-hierarchical-to-flat/`
**Предварительные условия**: `plan.md`, `spec.md`, `research.md`, `data-model.md`

**Организация**: Задачи сгруппированы по пользовательским историям для независимой реализации.

## Формат: `[ID] [P?] [Story] Описание`

- **[P]**: Может выполняться параллельно.
- **[Story]**: К какой пользовательской истории относится задача (например, US1, US2).

---

## Фаза 1: Настройка (Общая инфраструктура)

**Цель**: Подготовка структуры проекта для новой функциональности.

- [ ] T001 [P] [Setup] Создать файл `src/shield_ai/infrastructure/parsers/hierarchical_excel_parser.py` для нового парсера.
- [ ] T002 [P] [Setup] Создать файл `src/shield_ai/infrastructure/parsers/dto.py` для Data Transfer Objects.
- [ ] T003 [P] [Setup] Создать тестовый файл `tests/integration/test_hierarchical_excel_parser.py`.
- [ ] T004 [P] [Setup] Создать тестовый файл `tests/unit/parsers/test_hierarchical_excel_parser_units.py`.

---

## Фаза 2: Базовая (Блокирующие предварительные условия)

**Цель**: Реализация базовых моделей данных, необходимых для всех последующих задач.

- [ ] T005 [US1] [Baseline] Реализовать Pydantic-модель `FlatRecord` в `src/shield_ai/infrastructure/parsers/dto.py` в соответствии с `spec.md` (секция 7.2).
- [ ] T006 [US1] [Baseline] Реализовать Pydantic-модель для хранения контекста парсинга (например, `ParsingContext`) в `src/shield_ai/infrastructure/parsers/dto.py`.

**Контрольная точка**: Базовые модели данных готовы.

---

## Фаза 3: Пользовательская история 1 - Ядро парсера (Приоритет: P1) 🎯 MVP

**Цель**: Реализовать основную логику парсера для преобразования иерархического отчета в плоскую структуру данных.

**Независимый тест**: Запуск парсера на эталонном файле (`13.10.25-13.10.25 Полный все склады с коррекцией.xlsx`) и сверка результата с "golden master".

### Тесты для пользовательской истории 1 (TDD)
> **ПРИМЕЧАНИЕ: Пишите эти тесты СНАЧАЛА, убедитесь, что они НЕ ПРОХОДЯТ до реализации**

- [ ] T007 [US1] [Test] Написать юнит-тест в `tests/unit/parsers/test_hierarchical_excel_parser_units.py` для функции распознавания типов строк (заголовок, данные, итог).
- [ ] T008 [US1] [Test] Написать интеграционный тест в `tests/integration/test_hierarchical_excel_parser.py`, который проверяет полный цикл парсинга одного из эталонных файлов.

### Реализация для пользовательской истории 1

- [ ] T009 [US1] Реализовать в `hierarchical_excel_parser.py` класс `HierarchicalExcelParser`.
- [ ] T010 [US1] Реализовать в парсере логику итеративного чтения строк с использованием `openpyxl` в режиме `read-only`, как описано в `research.md`.
- [ ] T011 [US1] Реализовать конечный автомат и "контекстный стек" для управления иерархией (Склад, Товар, Документ, Партия).
- [ ] T012 [US1] Реализовать функцию распознавания типов строк на основе паттернов и структурных эвристик.
- [ ] T013 [US1] Реализовать логику извлечения данных из строк и заполнения `FlatRecord` на основе текущего контекста.
- [ ] T014 [US1] Обеспечить, чтобы парсер не применял вторичную логику (FIFO, агрегация), а только точно преобразовывал данные.

**Контрольная точка**: Ядро парсера функционально и проходит базовые тесты.

---

## Фаза 4: Пользовательская история 2 - Обработка ошибок и логирование (Приоритет: P2)

**Цель**: Обеспечить надежную обработку ошибок и подробное логирование для аудита и отладки.

**Независимый тест**: Запустить парсер на файле с заведомо некорректными данными и проверить, что ошибки корректно логируются.

### Реализация для пользовательской истории 2

- [ ] T015 [US2] Интегрировать стандартный логгер Python в `HierarchicalExcelParser`.
- [ ] T016 [US2] Реализовать логирование пропущенных или невосстановимых `batch_code`/`batch_date` с указанием координат строки.
- [ ] T017 [US2] Реализовать логирование некорректных структур (например, двойной заголовок, строка "итого").
- [ ] T018 [US2] Добавить обработку и логирование граничных случаев (пустые строки, отрицательные значения, дублирующиеся заголовки), как указано в `spec.md`.

**Контрольная точка**: Парсер устойчив к ошибкам и предоставляет информативные логи.

---

## Фаза 5: Пользовательская история 3 - Рефакторинг и доработка Streamlit UI (Приоритет: P3)

**Цель**: Обеспечить полное соответствие UI новой спецификации, включая расширенную обработку ошибок, пакетную загрузку и визуализацию.

**Независимый тест**: Загрузить batch из нескольких файлов через UI, проверить корректность отображения данных, логов ошибок и аналитики по усушке.

### Реализация для пользовательской истории 3

- [ ] T019 [US3] [UI] Интегрировать в `pages/1_parse.py` отображение и фильтрацию **error-log** (интерактивный просмотр, поиск) и реализовать **выгрузку всех ошибок одним файлом** (CSV/JSON) для BI.
- [ ] T020 [US3] [UI] Реализовать **batch upload** для обработки нескольких файлов, сводный просмотр ошибок и статуса по всем загруженным файлам.
- [ ] T021 [US3] [UI] Визуализировать в UI диапазоны инвентаризаций, виды движений и расчёт усушки в таблицах и графиках для аудита.
- [ ] T022 [US3] [UI] Добавить индикацию ошибок (количество строк в error-log, флаг "есть критичные ошибки") и версию схемы `FlatRecord` на каждой странице.
- [ ] T023 [US3] [UI] [Enhancement] Реализовать **агрегированный summary по batch-загрузке**: общая статистика, ключевые метрики и сводка по ошибкам.
- [ ] T024 [US3] [UI] [Enhancement] Выводить в UI **ключевые метрики**: количество успешно обработанных строк, количество ошибок, % корректности по каждому файлу и в целом по batch.
- [ ] T025 [US3] [UI] [UX] Реализовать **drag-and-drop** для загрузки файлов и добавить **информативный прогресс-бар** для отслеживания процесса обработки.

**Контрольная точка**: Streamlit-интерфейс полностью поддерживает новую функциональность и обеспечивает интерактивный аудит данных.

---

## Фаза 6: Пользовательская история 4 - E2E Тестирование и приемка (Приоритет: P2)

**Цель**: Гарантировать консистентность всего пайплайна и соответствие бизнес-логике через сверку с эталонными расчётами.

**Независимый тест**: Запуск автоматизированного E2E-теста, который эмулирует действия пользователя и сверяет конечный результат с "golden master".

### Реализация для пользовательской истории 4

- [ ] T026 [US4] [E2E/Acceptance] Разработать E2E тест-кейс для полуавтоматической сверки **"золотого эталона"** (golden master в CSV/JSON) с результатом работы пайплайна. Тест должен генерировать `diff`-файл при расхождениях.
- [ ] T027 [US4] [Test] Расширить E2E тест для проверки консистентности при batch-обработке нескольких файлов, сравнивая агрегированный результат с эталоном.

**Контрольная точка**: Сквозные тесты автоматизированы, включены в CI/CD и используют "golden master" для проверки корректности.

---

## Фаза 7: Пользовательская история 5 - Regression и Compatibility (Приоритет: P3)

**Цель**: Обеспечить обратную совместимость и подготовить план отката.

### Реализация для пользовательской истории 5

- [ ] T028 [US5] [Regression] Создать автоматизированный скрипт для тестирования совместимости новых экспортов с **legacy Streamlit UI** и существующими BI-интеграциями.
- [ ] T029 [US5] [Docs] Подготовить инструкцию по бэкапу и восстановлению старой версии UI/пайплайна (**rollback plan**).

**Контрольная точка**: План отката готов, обратная совместимость проверена.

---

## Фаза 8: Пользовательская история 6 - Production Monitoring/Ops (Приоритет: P3)

**Цель**: Внедрить базовые инструменты для мониторинга и диагностики в производственной среде.

### Реализация для пользовательской истории 6

- [ ] T030 [US6] [Ops/Monitoring] Внедрить логирование оперативных метрик пайплайна (время обработки, размер файла, количество ошибок) для будущей диагностики.
- [ ] T031 [US6] [Ops/Monitoring] Подготовить заглушку для интеграции каналов уведомлений (Telegram/email) о критичных ошибках (реализация по требованию).
- [ ] T032 [US6] [Ops/Monitoring] Реализовать **событийный журнал (event log)** для фиксации ключевых действий (запуск, завершение, ошибки) для аудита и поддержки.

**Контрольная точка**: Базовый мониторинг производительности и ошибок реализован.

---

## Фаза 9: Пользовательская история 7 - CLI и инфраструктура (Приоритет: P2)

**Цель**: Предоставить инструменты для автоматизированной обработки данных и интеграции в скрипты.

**Независимый тест**: Запустить CLI-команду для batch-парсинга папки и проверить, что сгенерирован корректный отчет об ошибках.

### Реализация для пользовательской истории 7

- [ ] T033 [US7] [CLI] Реализовать в `src/shield_ai/presentation/cli/main.py` команду для batch-парсинга папки с файлами.
- [ ] T034 [US7] [CLI] Добавить в CLI-команду опцию для **выгрузки всех ошибок в единый файл** (например, `errors.json`).
- [ ] T035 [US7] [CLI] Добавить в CLI-команду опцию для вывода **агрегированного summary** по результатам batch-обработки.
- [ ] T036 [US7] [CLI] Добавить в CLI-команду логирование основных событий (запуск/ошибка/успех).

**Контрольная точка**: CLI-интерфейс готов для использования в CI/CD и для ручного запуска.

---

## Фаза 10: Пользовательская история 8 - Документация и UX/Onboarding (Приоритет: P3)

**Цель**: Обеспечить пользователей и команду поддержки всей необходимой документацией для работы с новой версией.

### Реализация для пользовательской истории 8

