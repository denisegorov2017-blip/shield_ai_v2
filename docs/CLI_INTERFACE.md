# 🚀 Shield AI v2.0 - CLI Интерфейс

## 1. Введение

Интерфейс командной строки (CLI) Shield AI предоставляет возможность взаимодействия с основными функциями системы без использования графического интерфейса. Это особенно полезно для автоматизации задач, интеграции с другими системами и выполнения операций на сервере.

Основной исполняемый файл CLI находится по пути: [`src/shield_ai/presentation/cli/main.py`](src/shield_ai/presentation/cli/main.py)

## 2. Основные команды

CLI построен с использованием библиотеки `Click` и предоставляет следующие основные команды:

### 2.1. `parse` - Импорт данных инвентаризации

Эта команда используется для импорта данных инвентаризации из различных источников (например, Excel файлов) в систему.

**Использование:**
```bash
python src/shield_ai/presentation/cli/main.py parse [OPTIONS]
```

**Опции:**
*   `--file PATH`: Путь к файлу инвентаризации для парсинга. (Обязательно)
*   `--format [excel|csv|json]`: Формат входного файла. (По умолчанию: `excel`)

**Примеры:**
```bash
# Импорт данных из Excel файла
python src/shield_ai/presentation/cli/main.py parse --file data/input/inventory.xlsx --format excel

# Импорт данных из CSV файла
python src/shield_ai/presentation/cli/main.py parse --file data/input/inventory.csv --format csv
```

### 2.2. `calibrate` - Калибровка коэффициентов усушки

Команда для запуска процесса калибровки коэффициентов усушки (`a`, `b`, `c`) для каждого SKU на основе исторических данных инвентаризации.

**Использование:**
```bash
python src/shield_ai/presentation/cli/main.py calibrate [OPTIONS]
```

**Опции:**
*   `--sku TEXT`: SKU продукта, для которого нужно откалибровать коэффициенты. Если не указано, калибруются все SKU.
*   `--min-inventories INTEGER`: Минимальное количество инвентаризаций, необходимое для калибровки. (По умолчанию: `3`)

**Примеры:**
```bash
# Калибровка коэффициентов для конкретного SKU
python src/shield_ai/presentation/cli/main.py calibrate --sku "SKU12345"

# Калибровка коэффициентов для всех SKU с минимум 5 инвентаризациями
python src/shield_ai/presentation/cli/main.py calibrate --min-inventories 5
```

### 2.3. `forecast` - Прогнозирование усушки

Эта команда позволяет прогнозировать усушку для заданных партий товаров, используя откалиброванные коэффициенты и выбранную стратегию.

**Использование:**
```bash
python src/shield_ai/presentation/cli/main.py forecast [OPTIONS]
```

**Опции:**
*   `--batch-id TEXT`: ID партии товара для прогнозирования. (Обязательно)
*   `--strategy [portion|weighted|final]`: Стратегия расчёта усушки. (По умолчанию: `weighted`)

**Примеры:**
```bash
# Прогнозирование усушки для партии с ID "BATCH001" с использованием взвешенной стратегии
python src/shield_ai/presentation/cli/main.py forecast --batch-id "BATCH001" --strategy weighted
```

### 2.4. `audit` - Аудит остатков инвентаризации

Команда для проведения аудита остатков на складе, выявления отрицательных и избыточных балансов.

**Использование:**
```bash
python src/shield_ai/presentation/cli/main.py audit [OPTIONS]
```

**Опции:**
*   `--date TEXT`: Дата, на которую проводится аудит (формат YYYY-MM-DD). (Обязательно)
*   `--output-format [json|markdown]`: Формат вывода отчета. (По умолчанию: `markdown`)

**Примеры:**
```bash
# Аудит остатков на 2025-11-15, вывод в Markdown
python src/shield_ai/presentation/cli/main.py audit --date 2025-11-15 --output-format markdown

# Аудит остатков на 2025-11-15, вывод в JSON
python src/shield_ai/presentation/cli/main.py audit --date 2025-11-15 --output-format json
```

## 3. Общие опции CLI

Все команды CLI могут поддерживать общие опции, такие как:

*   `--help`: Отображает справочную информацию для команды.
*   `--version`: Отображает версию приложения.

**Пример:**
```bash
python src/shield_ai/presentation/cli/main.py --help
python src/shield_ai/presentation/cli/main.py parse --help