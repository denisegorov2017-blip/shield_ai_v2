<!-- Version change: 2.0 → 2.1 -->
<!-- Date: 2025-11-14 -->
<!-- Author: Denis Egorov -->
<!-- Modified principles:
  - I. Библиотечный подход (расширено)
  - II. Интерфейс командной строки (расширено)
  - III. Тестирование первым (НЕОБСУЖДАЕМОЕ) (расширено)
  - IV. Интеграционное тестирование (расширено)
  - V. Наблюдаемость (расширено)
  - VI. Версионирование и критические изменения (расширено)
  - VII. Простота (YAGNI + Правило трёх) (расширено)
  - VIII. Синхронность (НЕОБСУЖДАЕМОЕ) (добавлено)
-->
<!-- Added sections:
  - Архитектурные требования (Clean Architecture, прагматичные принципы)
  - Технологический стек и качество кода (инструменты, типизация, метрики)
  - Рабочий процесс разработки (Git Flow, Code Review, CI/CD, Makefile)
  - Правила работы с ИИ-агентами (взаимодействие, ограничения, workflow)
  - Документация (обязательные документы, docstrings, комментарии)
-->
<!-- Removed sections: none -->
<!-- Templates requiring updates: ✅ .kilocode/rules/Ruls_prodject.md -->
<!-- Follow-up TODOs: none -->

# Конституция проекта shield_ai_v2

<!-- Intelligent Inventory Management System with AI-powered shrinkage prediction | Clean Architecture + SQLAlchemy 2.0 + Streamlit -->

## Основные принципы

### I. Библиотечный подход

Каждая функция начинается как автономная библиотека; Библиотеки должны быть автономными, независимо тестируемыми, документированными с публичным API; Требуется четкая бизнес-цель - запрещены библиотеки только для организации кода; Структура: `src/shield_ai/domain/` (бизнес-логика), `application/` (use cases), `infrastructure/` (БД, парсеры), `presentation/` (UI)

<!-- Пример: domain/shrinkage/ - библиотека моделей усушки с публичным API в __init__.py, функциями расчета в strategies.py и доменными моделями в models.py; Начинаем с простых функций, превращаем в классы когда появится сложность -->

### II. Интерфейс командной строки

Каждая библиотека предоставляет функциональность через CLI для автоматизации и тестирования; Протокол текстового ввода/вывода: stdin/args → stdout (результат), ошибки → stderr (логи); Поддержка JSON для машинного чтения и человекочитаемых форматов для отладки; Структурированное логирование в JSON формате обязательно; CLI реализуется параллельно с основной логикой

<!-- Пример: python -m shield_ai.application.calibrate --input data/batch_data.json --output results/coefficients.json --strategy linear --format json; CLI позволяет вызывать use cases без UI -->

### III. Тестирование первым (НЕОБСУЖДАЕМОЕ)

TDD обязательно для всего кода: Цикл Red-Green-Refactor (тест не проходит → минимальная реализация → рефакторинг); Тесты пишутся ДО реализации; Требуется явное одобрение пользователем после написания тестов; Без одобренных тестов реализация ЗАПРЕЩЕНА; Минимальное покрытие 80%, критические модули (domain, application) 95%; Тесты - это документация и контракт; Каждый коммит должен содержать тесты

<!-- Структура: tests/unit/ (изолированные модульные тесты), tests/integration/ (контракты между слоями), tests/e2e/ (end-to-end через Streamlit UI); Команда запуска: make test; TDD важнее идеального дизайна - сначала работающий код с тестами, потом рефакторинг -->

### IV. Интеграционное тестирование

Области, требующие интеграционных тестов: Тестирование контрактов новых библиотек при добавлении; Изменения контрактов существующих библиотек (breaking changes); Межслойное взаимодействие (domain ↔ infrastructure, application ↔ presentation); Общие схемы данных (DTO, ORM модели SQLAlchemy); Парсеры внешних данных (Excel, CSV); Взаимодействие UI с application layer; Интеграция с внешними API

<!-- Пример: test_repository_contract() проверяет, что ProductRepository корректно реализует интерфейс из domain; test_excel_parser_integration() проверяет парсинг реальных Excel файлов; test_streamlit_page_integration() проверяет взаимодействие UI со use cases -->

### V. Наблюдаемость

Текстовый ввод/вывод обеспечивает отладку через CLI; Требуется структурированное ведение логов в JSON формате для парсинга; Уровни: DEBUG (детали разработки), INFO (ключевые операции), WARNING (предупреждения), ERROR (ошибки требуют внимания), CRITICAL (система не работает); Логирование всех критических операций с полным контекстом (user_id, request_id, timestamps); Трассировка ошибок с stack trace обязательна; Метрики производительности для критических операций

<!-- Формат лога: {"timestamp": "2025-11-14T04:32:00+03:00", "level": "INFO", "module": "shield_ai.application.calibrate", "message": "Calibration completed", "duration_ms": 234, "data": {"product_id": 123, "strategy": "linear", "coefficient": 0.023}}; Используем стандартный logging с structlog для JSON -->

### VI. Версионирование и критические изменения

Формат версий: MAJOR.MINOR.PATCH (breaking.compatible.fixes); MAJOR - критические изменения API/интерфейсов (breaking changes); MINOR - новый функционал обратно совместимый (новые use cases, UI страницы); PATCH - исправления ошибок без изменения API; При breaking changes: документировать в CHANGELOG.md, обновить pyproject.toml, создать план миграции для пользователей, обновить всю документацию, написать migration guide

<!-- Текущая версия проекта: 2.0.0; Пример breaking change: изменение формата DTO между application и presentation слоями требует MAJOR версии; Используем семантическое версионирование (semver) -->

### VII. Простота (YAGNI + Правило трёх)

Начинать с простого, добавлять сложность только при необходимости и с обоснованием; Не реализовывать функции "на будущее" или "может пригодится"; **Правило трёх**: абстракция добавляется только после третьего дублирования кода; Каждая абстракция и сложность должна быть обоснована конкретной бизнес-задачей; Предпочитать явное неявному (explicit is better than implicit); Избегать преждевременной оптимизации (optimize when measured, not before); Применять принципы YAGNI (You Aren't Gonna Need It) и KISS (Keep It Simple, Stupid)

<!-- Принцип: простое работающее решение сейчас лучше сложного "универсального" решения "на всякий случай"; Если сомневаешься - выбирай проще; Пример: начинаем с функций, переходим к классам когда появляется состояние; начинаем с if/else, переходим к Strategy Pattern когда будет 5+ стратегий -->

### VIII. Синхронность (НЕОБСУЖДАЕМОЕ)

Весь код пишется СИНХРОННО - без async/await, asyncio, корутин; SQLAlchemy 2.0 используется в синхронном режиме (Session, не AsyncSession); Streamlit работает синхронно по умолчанию - не усложняем; Pandas, Excel парсинг, файловые операции - всё синхронно; Use cases синхронные функции/классы; Асинхронность добавляется ТОЛЬКО при измеренной необходимости (>100 RPS, конкретные проблемы производительности) и с явным обоснованием; Синхронный код проще отлаживать, тестировать, понимать

<!-- Принцип: синхронный код для 95% случаев достаточен; async добавляем только когда синхронный код не справляется (профилирование показало узкое место); Streamlit + SQLAlchemy sync = простота -->

## Архитектурные требования

**Clean Architecture (обязательно)**: Направление зависимостей: Presentation → Application → Domain ← Infrastructure; Domain не зависит ни от кого (чистая бизнес-логика без знания о БД/UI/фреймворках); Application зависит только от Domain (use cases оркестрируют доменную логику); Infrastructure реализует интерфейсы Domain (SQLAlchemy репозитории, Excel парсеры); Presentation использует Application (Streamlit UI вызывает use cases); **Главное правило**: зависимости идут только внутрь, от внешних слоёв к внутренним

**Прагматичные архитектурные принципы (вместо строгого SOLID)**:

**Обязательно соблюдать**:
- **Dependency Inversion**: Use cases зависят от интерфейсов, не от конкретных реализаций (позволяет менять БД/хранилище без переписывания логики)
- **Single Responsibility**: Модуль/класс/функция решает одну понятную задачу (упрощает тестирование и понимание)
- **Separation of Concerns**: Бизнес-логика отдельно от UI, отдельно от БД, отдельно от внешних API
- **Явные зависимости**: Все зависимости передаются явно (через конструктор или параметры), не через глобальные переменные

**Применять по необходимости (не заранее)**:
- **Strategy Pattern**: Когда будет 5+ стратегий/алгоритмов (до этого - обычные функции)
- **Factory Pattern**: Когда создание объектов станет сложным с множественными условиями
- **Observer/Events**: Когда нужна реактивность и события между компонентами
- **Repository Pattern**: Используем для БД, но без излишних абстракций (один репозиторий на aggregate)

**Принципы рефакторинга (эволюция кода)**:
1. Пишем простой работающий код (функции, if/else, прямолинейная логика)
2. Покрываем тестами 80%+ (тесты - документация и защита)
3. Рефакторим когда появляется **реальная** необходимость: дублирование 3+ раз, сложность растёт, появляются баги
4. Не оптимизируем без измерений (профилирование → оптимизация)

<!-- Современный подход: Start Simple, Refactor When Needed, Test Everything; Пример эволюции: функция → класс с состоянием → паттерн Strategy → паттерн Factory (но только когда реально нужно) -->

**Структура слоев**:
- `domain/` - entities (dataclasses для данных), repositories (интерфейсы - простые Protocol классы), shrinkage models (функции расчета, классы только при необходимости состояния), validation (функции валидации с понятными ошибками)
- `application/` - use_cases (классы с одним методом execute() или функции для простых случаев), dto (Pydantic модели для валидации)
- `infrastructure/` - database (SQLAlchemy 2.0 ORM модели с Mapped, один репозиторий на aggregate root), excel (функции парсинга Excel/CSV), config (простой словарь с настройками или Pydantic Settings)
- `presentation/` - ui/pages (Streamlit страницы вызывают use cases, минимум логики в UI)

**Единая точка входа**: `main.py` запускает Streamlit приложение; Все страницы UI находятся в `src/shield_ai/presentation/ui/pages/`; Навигация через Streamlit multipage (автоматическая); Каждая страница вызывает один или несколько use cases из application layer; Прямой доступ к domain/infrastructure из UI ЗАПРЕЩЁН; UI страница = тонкий слой над use cases

<!-- Пример: pages/calibration.py вызывает CalibrateCoefficientsUseCase.execute(), отображает результат в Streamlit, обрабатывает ошибки; Вся бизнес-логика в use case, UI только отображение -->

## Технологический стек и качество кода

**Основные технологии**: Python ^3.11 (современный Python с улучшенной типизацией); SQLAlchemy ^2.0 (ORM с Mapped типизацией, синхронный для простоты); Alembic ^1.12 (миграции БД с автогенерацией); Streamlit ^1.28 (единственный UI, multipage, точка входа); Pandas ^2.0 (обработка данных, работа с Excel); Pydantic ^2.5 (валидация DTO, настройки); python-dotenv ^1.0 (конфигурация через .env); openpyxl ^3.1 (чтение Excel); scipy ^1.11 (статистика для моделей усушки); plotly ^5.17 (интерактивные графики в Streamlit)

**Обязательные инструменты разработки**:
- MyPy ^1.7.0 (статическая типизация, strict mode, ОБЯЗАТЕЛЬНО - защита от багов на этапе разработки)
- Ruff ^0.1.0 (современный быстрый линтер с автофиксом, ОБЯЗАТЕЛЬНО - заменяет Flake8 + isort + множество плагинов)
- Black ^24.3.0 (форматирование, line-length=88, ОБЯЗАТЕЛЬНО - конец споров о стиле)
- Pytest ^7.4.0 + pytest-cov ^4.1.0 (тестирование с покрытием, ОБЯЗАТЕЛЬНО)
- Pre-commit ^3.5.0 (git hooks для автопроверки перед коммитом, ОБЯЗАТЕЛЬНО)
- Bandit ^1.7.0 (проверка безопасности кода, ОБЯЗАТЕЛЬНО - SQL инъекции, eval, пароли)
- Pylint ^3.0.0 (глубокий анализ качества, рекомендуется - метрики сложности)
- Pyright ^1.1.300 (дополнительная проверка типов от Microsoft, опционально)
- pip-audit ^2.9.0 (проверка уязвимостей зависимостей, ОБЯЗАТЕЛЬНО)
- radon ^6.0.1 (метрики сложности кода, рекомендуется)

**Современный подход к типизации (MyPy strict mode)**:
- Все публичные функции ОБЯЗАТЕЛЬНО имеют type hints (приватные можно без hints если очевидно)
- Использование современных конструкций: `list[str]` вместо `List[str]`, `dict[str, int]` вместо `Dict[str, int]`
- `from __future__ import annotations` для forward references
- Использование `Protocol` для интерфейсов вместо ABC когда не нужна реализация
- Использование `TypedDict` для словарей с известной структурой
- `Mapped[Type]` и `mapped_column()` для SQLAlchemy 2.0 моделей
- Маркер `py.typed` присутствует во всех библиотечных пакетах
- `Any` разрешён только с комментарием-обоснованием: `value: Any  # External API returns untyped data`
- `type: ignore` только с кодом ошибки и объяснением: `# type: ignore[attr-defined]  # Dynamic attribute from metaclass`

**Конфигурация MyPy** (в pyproject.toml):
```toml
[tool.mypy]
python_version = "3.11"
strict = true
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true  # Только для публичных функций
allow_untyped_decorators = false
namespace_packages = true
explicit_package_bases = true
mypy_path = "src"
plugins = ["pydantic.mypy", "sqlalchemy.ext.mypy.plugin"]
```

**Проверка качества кода (обязательная последовательность)**:
1. `make format` - автоформатирование (Black + Ruff --fix для isort)
2. `make lint` - проверка стиля (Ruff с ~700 правилами, быстрее Flake8 в 10-100x)
3. `make typecheck` - проверка типов (MyPy strict + опционально Pyright)
4. `make security` - проверка безопасности (Bandit для кода, pip-audit для зависимостей)
5. `make test` - запуск тестов (Pytest с параллелизацией через pytest-xdist)
6. `make coverage` - отчет покрытия (проверка >= 80%)
7. `make complexity` - метрики сложности (Radon, опционально)
8. `make all` - полная проверка перед коммитом (все команды + pre-commit hooks)

**Требования к покрытию тестами**:
- Минимальное общее покрытие: **80%** (проект не проходит CI без этого)
- Критические модули (domain/, application/): **95%** обязательно
- Простые модули (dto, configs): можно 70%+ если нет сложной логики
- Отчеты генерируются в форматах: HTML (для просмотра), XML (для CI/CD), terminal (для быстрой проверки)
- Команда: `pytest --cov=src/shield_ai --cov-report=html --cov-report=xml --cov-report=term --cov-fail-under=80 -n auto`
- Просмотр: `open htmlcov/index.html` или `make coverage && make serve-coverage`

**Современные метрики качества кода**:
- **Циклическая сложность** (Cyclomatic Complexity): максимум **10** на функцию (рекомендация 1-5)
- **Cognitive Complexity**: максимум **15** на функцию (насколько код сложен для понимания)
- **Максимальная вложенность блоков**: **4** уровня (if внутри if внутри for внутри with = 4)
- **Максимальная длина функции**: **50** строк код (не считая docstrings и пустые строки)
- **Максимальное количество параметров**: **5** для функции (больше - использовать dataclass/NamedTuple)
- Проверка: `radon cc src/shield_ai -s -a -n 10 && radon mi src/shield_ai -s` (средняя сложность A или B)

<!-- Современный подход: автоматизация проверок (pre-commit), быстрые инструменты (Ruff вместо Flake8), фокус на читаемости (Cognitive Complexity важнее Cyclomatic) -->

## Рабочий процесс разработки

**Git Flow (упрощённый)**:
- Ветки: `main` (production-ready, защищённая), `develop` (интеграция), `feature/XXX-description` (новые функции), `bugfix/XXX-description` (исправления), `hotfix/XXX-description` (срочные правки в production)
- **Современный формат коммитов** (Conventional Commits): `<type>(<scope>): <subject>` + опционально тело и footer
- **Типы**: `feat` (новая функция), `fix` (исправление бага), `docs` (документация), `style` (форматирование без изменения логики), `refactor` (рефакторинг без изменения поведения), `perf` (оптимизация производительности), `test` (добавление/изменение тестов), `build` (изменения в сборке/зависимостях), `ci` (изменения в CI/CD), `chore` (обслуживание, рутинные задачи)
- **Примеры**: `feat(calibration): add exponential shrinkage strategy`, `fix(parser): handle empty Excel cells correctly`, `refactor(repository): simplify query builder`, `docs(api): update use case examples`
- Запрещены коммиты напрямую в main - только через PR с ревью и проходящими тестами

**Code Review (фокус на главном)**:

**Критичные проверки (блокируют merge)**:
1. ✅ **Тесты**: TDD цикл пройден, все тесты проходят, покрытие >= 80% (domain/application >= 95%)
2. ✅ **Типизация**: MyPy strict без ошибок, все публичные функции типизированы
3. ✅ **Архитектура**: Clean Architecture соблюдена, зависимости направлены правильно, UI не обращается к domain/infrastructure
4. ✅ **Безопасность**: Bandit 0 критичных уязвимостей, pip-audit 0 критичных уязвимостей в зависимостях

**Важные проверки (желательно исправить)**:
5. ⚠️ **Линтер**: Ruff минимум ошибок (допустимо 1-2 несущественных), Pylint >= 8.5/10
6. ⚠️ **Сложность**: Функции CC <= 10, вложенность <= 4, длина <= 50 строк
7. ⚠️ **Документация**: Публичные API имеют docstrings, README/CHANGELOG обновлены
8. ⚠️ **Логирование**: Критические операции логируются с контекстом

**Необязательные проверки (можно в следующем PR)**:
9. 💡 **Оптимизация**: Производительность (только если есть измеренная проблема)
10. 💡 **Покрытие 95%+**: Для некритичных модулей (если есть время)

**Критерии автоматического отклонения PR**:
- ❌ Тесты не проходят или их нет
- ❌ MyPy strict выдаёт ошибки типизации
- ❌ Bandit или pip-audit находят критичные уязвимости
- ❌ Покрытие < 80% (или < 95% для domain/application)
- ❌ Нарушена Clean Architecture (например, UI → Infrastructure напрямую)
- ❌ Streamlit страница обращается к domain/infrastructure минуя use cases

**CI/CD Pipeline (GitHub Actions, оптимизированный)**:

**Stage 1: Fast Checks** (1-2 минуты, fail fast):
- Ruff lint (быстрая проверка стиля)
- MyPy typecheck (проверка типов)
- Pytest unit tests только (быстрые тесты без внешних зависимостей)

**Stage 2: Deep Checks** (3-5 минут, если Stage 1 прошёл):
- Pylint (глубокий анализ качества)
- Pyright (дополнительная проверка типов)
- Bandit + pip-audit (безопасность)
- Pytest integration + e2e tests (медленные тесты с БД)
- Coverage report (проверка >= 80%)

**Stage 3: Quality** (опционально, не блокирует merge):
- Radon complexity metrics (генерация отчёта)
- Performance benchmarks (если есть)
- Documentation build (Sphinx)

**Stage 4: Deploy** (только для main ветки):
- Build package (Poetry build)
- Deploy to staging (автоматически)
- Deploy to production (manual approval)

<!-- Современный подход: fail fast (быстрые проверки сначала), параллелизация (Stage 1 и 2 параллельно где возможно), кэширование (pip cache, mypy cache) -->

**Makefile команды (стандартизированный интерфейс)**:
```makefile
# Установка и настройка
make install          # Установка зависимостей через Poetry
make install-dev      # Установка dev зависимостей
make setup            # Первичная настройка (install + pre-commit)

# Форматирование и проверка
make format           # Black + Ruff --fix (автоформат)
make lint             # Ruff + Pylint (проверка стиля)
make typecheck        # MyPy + Pyright (типы)
make security         # Bandit + pip-audit (безопасность)

# Тестирование
make test             # Pytest unit + integration + e2e
make test-unit        # Только юнит тесты (быстро)
make test-integration # Только интеграционные
make coverage         # Отчёт покрытия + открыть HTML
make watch-test       # Непрерывный запуск тестов (pytest-watch)

# Качество кода
make complexity       # Radon метрики сложности
make quality          # Полный анализ качества (lint + typecheck + complexity)

# Документация
make docs             # Генерация Sphinx документации
make serve-docs       # Запуск локального сервера документации

# Комплексные команды
make check            # Быстрая проверка (format + lint + test-unit)
make all              # Полная проверка перед коммитом (format + quality + test + coverage)
make ci               # Эмуляция CI pipeline локально

# Очистка
make clean            # Удаление артефактов сборки
make clean-all        # Полная очистка (включая .venv)

# Разработка
make run              # Запуск Streamlit приложения
make shell            # Открыть Poetry shell
make migrate          # Применить миграции Alembic
make migrate-create   # Создать новую миграцию
```

<!-- Makefile - единая точка входа для всех команд, документация через `make help` -->

## Правила работы с ИИ-агентами (spec-kit / KiloCode)

**Обязательные файлы перед началом работы**:
- `spec.md` - спецификация задачи (что нужно сделать, зачем, критерии приёмки)
- `plan.md` - детальный план выполнения по шагам с оценкой времени
- Файлы должны быть актуальны, согласованы и **явно утверждены пользователем** перед началом

**Современный подход к работе с ИИ-агентами**:

**Принципы взаимодействия**:
1. **Итеративность**: Небольшие итерации с проверкой результата (лучше 5 маленьких PR чем 1 большой)
2. **Явная коммуникация**: ИИ-агент сообщает что делает, запрашивает подтверждение на критичные действия
3. **Воспроизводимость**: Все действия документируются, можно повторить вручную
4. **Безопасность**: ИИ-агент не имеет прямого доступа к production, все изменения через PR

**Правила выполнения (СТРОГО)**:
1. ✅ Любая задача начинается с актуальных `spec.md` и `plan.md` (утверждённых пользователем)
2. ✅ Выполнение только через утверждённые команды из `Makefile` или документации
3. ❌ ЗАПРЕЩЕНО добавлять/менять шаги вне `plan.md` без явного одобрения
4. ✅ Каждый шаг сверяется с критериями приёмки из `spec.md`
5. ✅ История выполнения фиксируется: логи, комментарии к коммитам, issue updates
6. ⛔ При ошибке/неясности - ОСТАНОВКА, информирование пользователя, создание issue
7. ✅ Эксперименты/refactoring - только в feature ветках с отдельными spec/plan
8. ✅ Результаты должны быть воспроизводимы (инструкции в PR description)
9. ✅ Документация обновляется автоматически (docstrings → Sphinx, CHANGELOG → git log)
10. ❌ ЗАПРЕЩЕНО автоисправление без одобрения (критичные ошибки → issue → обсуждение → fix)

**Ограничения для ИИ-агентов при работе с кодом**:

**ЗАПРЕЩЕНО**:
- ❌ Создавать Streamlit страницы, обращающиеся к domain/infrastructure напрямую (только через use cases)
- ❌ Добавлять сложные абстракции "на будущее" (Strategy/Factory/Builder без реальной необходимости)
- ❌ Рефакторить код без тестов или при покрытии < 80%
- ❌ Коммитить код с ошибками MyPy/Ruff/Bandit
- ❌ Создавать PR без обновления документации (docstrings, CHANGELOG, README)
- ❌ Использовать deprecated функции SQLAlchemy/Pandas/Streamlit

**ОБЯЗАТЕЛЬНО**:
- ✅ TDD цикл: тест → одобрение → реализация → рефакторинг
- ✅ Начинать с простого: функция → класс → паттерн (только при необходимости)
- ✅ Проверять перед коммитом: `make all` должен проходить без ошибок
- ✅ Логировать критичные операции с контекстом (product_id, user_id, duration)
- ✅ Обрабатывать ошибки явно (не голый except:, конкретные исключения)
- ✅ Документировать публичные API (docstrings с примерами)

**Workflow для ИИ-агента**:
```bash
# 1. Получить задачу (spec.md + plan.md утверждены)
# 2. Создать feature ветку
git checkout -b feature/XXX-description

# 3. TDD цикл для каждого шага
make test-unit              # Убедиться что база работает
# Написать тест
pytest tests/unit/test_new_feature.py  # Тест падает (Red)
# Написать минимальную реализацию
pytest tests/unit/test_new_feature.py  # Тест проходит (Green)
# Рефакторинг (если нужен)
make all                    # Всё проходит

# 4. Проверка перед коммитом
make all                    # Форматирование, линтер, типы, тесты, покрытие

# 5. Коммит (pre-commit hooks запустятся автоматически)
git add .
git commit -m "feat(module): add new feature"

# 6. Пуш и создание PR
git push origin feature/XXX-description
# Создать PR через GitHub CLI или web interface
gh pr create --title "feat: add new feature" --body "Implements #123"

# 7. Ждать review, исправить замечания если есть
```

**Директория управления**:
- `.kilocode/rules/Ruls_prodject.md` - специфичные правила для KiloCode агентов
- `.kilocode/workflows/` - автоматизированные рабочие процессы
- `.specify/` - спецификации для spec-kit (альтернатива spec.md)
- `.github/workflows/` - CI/CD конфигурация (GitHub Actions)
- Изменения в `.kilocode/` требуют обновления Конституции с обоснованием

<!-- Современный подход: ИИ-агенты как младшие разработчики - они пишут код, но человек проверяет и одобряет; Автоматизация рутины (форматирование, линтинг), человек для критичных решений (архитектура, безопасность) -->

## Документация

**Обязательные документы** (живая документация, обновляется автоматически где возможно):
- `README.md` - краткое описание, установка, быстрый старт (обновлять при изменении функционала)
- `QUICK_START.md` - пошаговое руководство для новых разработчиков (обновлять при изменении setup)
- `docs/ARCHITECTURE.md` - Clean Architecture, структура слоёв, диаграммы (обновлять при рефакторинге)
- `docs/RUNNING_TESTS.md` - запуск тестов, интерпретация coverage, отладка (обновлять при изменении тестов)
- `docs/UI_PAGES.md` - Streamlit страницы, навигация, какие use cases используются (автогенерация из кода возможна)
- `CHANGELOG.md` - история изменений (можно генерировать из Conventional Commits)
- `CONTRIBUTING.md` - руководство для контрибьюторов (обновлять при изменении процесса)
- `CODE_OF_CONDUCT.md` - правила поведения в проекте (опционально для open source)

**Современный подход к docstrings**:

**Обязательно документировать**:
- ✅ Все публичные функции/классы/метомы (экспортируемые из `__init__.py`)
- ✅ Use cases (что делает, какие данные принимает, что возвращает, какие исключения)
- ✅ Domain entities (назначение, инварианты, бизнес-правила)
- ✅ Сложные алгоритмы (математика, оптимизации, нетривиальная логика)

**Можно не документировать** (код самодокументируемый):
- ⚪ Приватные функции с очевидной логикой (`_format_date`, `_validate_input`)
- ⚪ Простые DTO/dataclasses (type hints достаточно)
- ⚪ Тривиальные getter/setter (если они просто возвращают/устанавливают значение)
- ⚪ Тесты (имена тестов должны быть самодокументирующими: `test_calibrate_raises_error_when_data_empty`)

**Формат docstrings** (Google Style, краткий и полезный):
```python
def calibrate_coefficient(
    batch_data: list[BatchData],
    strategy: ShrinkageStrategy,
    *,  # Keyword-only arguments
    min_samples: int = 30
) -> float:
    """Calibrate shrinkage coefficient from historical data.

    Analyzes batch history and calculates optimal shrinkage coefficient

    Args:
        batch_data: Historical batch data with actual shrinkage.
            Minimum 30 samples required for statistical significance.
        strategy: Shrinkage calculation strategy (Linear/Exponential/Seasonal).
        min_samples: Minimum samples for calibration. Default 30.

    Returns:
        Calibrated shrinkage coefficient in [0.0, 1.0] range.
        Represents fraction of natural weight loss.

    Raises:
        ValueError: If batch_data is empty or has < min_samples records.
        ValueError: If data contains invalid values (NaN, negative).

    Example:
        >>> data = [BatchData(100, 97, 7), BatchData(50, 48.5, 7)]
        >>> strategy = LinearShrinkageStrategy()
        >>> coeff = calibrate_coefficient(data, strategy)
        >>> print(f"{coeff:.4f}")
        0.0234
    """
```

**Автогенерация документации** (Sphinx + автоматизация):
- Sphinx с autodoc плагином (генерация API reference из docstrings)
- Napoleon расширение для поддержки Google Style docstrings
- Markdown support через myst-parser (можно писать docs в Markdown)
- Автоматическая генерация в CI/CD (deploy на GitHub Pages или Read the Docs)
- Команды: `make docs` (генерация), `make serve-docs` (локальный просмотр на http://localhost:8000)
- Конфигурация: `docs/conf.py` (Sphinx настройки), `docs/index.rst` (главная страница)

**Современный подход к комментариям**:

**Пишем комментарии для**:
- 🎯 **ПОЧЕМУ** (не ЧТО): объяснение нетривиальных решений, бизнес-правил, workarounds
- 🎯 **Контекст**: ссылки на issue, документацию, RFC, внешние источники
- 🎯 **Предупреждения**: сложные места, возможные подводные камни, граничные случаи

**НЕ пишем комментарии для**:
- ❌ Очевидного кода: `x = x + 1  # Increment x` - бесполезно
- ❌ Устаревшей информации: комментарии должны обновляться вместе с кодом или удаляться
- ❌ Закомментированного кода: используем git history вместо комментирования старого кода

**Современные стандарты комментариев**:
```python
# ✅ ХОРОШО: Объясняет ПОЧЕМУ
# Using exponential moving average instead of simple moving average
# because it reacts faster to recent changes in shrinkage patterns.
# See research paper: https://doi.org/10.1234/shrinkage-2024
coefficient = calculate_ema(data, alpha=0.3)

# ✅ ХОРОШО: TODO с issue номером
# TODO(#123): Optimize this query using window functions
# Currently O(n²), should be O(n log n) with proper indexing
result = slow_aggregation(data)

# ✅ ХОРОШО: Предупреждение о граничном случае
# NOTE: This assumes all weights are positive and non-zero.
# If weight can be zero, caller must validate before calling.
ratio = initial_weight / final_weight

# ❌ ПЛОХО: Очевидный комментарий
# Get product by ID
product = repository.get_by_id(product_id)

# ❌ ПЛОХО: TODO без issue
# TODO: maybe refactor this later

# ❌ ПЛОХО: Закомментированный код (удали, git помнит)
# old_coefficient = calculate_old_way(data)
# if old_coefficient > threshold:
#     return old_coefficient
```

**Type hints комментарии** (только когда MyPy не может вывести):
```python
from typing import Any, cast

# ✅ ХОРОШО: Объяснение почему Any
data: Any  # External API returns untyped JSON, validated with Pydantic later

# ✅ ХОРОШО: Объяснение type ignore
result = dynamic_lookup(key)  # type: ignore[attr-defined]  # Dynamic attr from __getattr__

# ✅ ХОРОШО: Cast с объяснением
user_id = cast(int, session.get("user_id"))  # Validated in middleware, always int here
```

<!-- Современный подход: код должен быть самодокументируемым (понятные имена, типы, структура), комментарии для неочевидного (ПОЧЕМУ, контекст, предупреждения) -->

## Управление

**Иерархия документов** (от высшего авторитета к низшему):
1. 🏛️ **Конституция проекта** (этот документ) - превалирует над всеми остальными
2. ⚙️ `.kilocode/rules/Ruls_prodject.md` - правила для ИИ-агентов в runtime
3. 🏗️ `docs/ARCHITECTURE.md` - архитектурные решения и паттерны
4. 🎨 `docs/UI_PAGES.md` - спецификация Streamlit UI и use cases
5. 📝 Локальные `README.md` в модулях - детали конкретных компонентов
6. 💬 Docstrings в коде - документация API уровня функций/классов
7. 🗣️ Комментарии в коде - контекст и объяснения конкретных решений

**Процесс изменения Конституции**:

**Когда требуется изменение**:
- ⚠️ Текущие принципы блокируют развитие проекта
- ⚠️ Появились новые технологии/практики, улучшающие разработку
- ⚠️ Обнаружены противоречия или неясности в формулировках
- ⚠️ Накопились технические долги из-за устаревших требований

**Процедура изменения** (строгая):
1. **Создание issue** - детальное описание проблемы и предлагаемого изменения
2. **Обоснование** - почему текущий подход не работает, какие преимущества даст изменение, риски
3. **Обсуждение** - в комментариях к issue, с примерами кода, метриками, ссылками на статьи/исследования
4. **Утверждение** - владелец проекта одобряет изменение (или отклоняет с объяснением)
5. **План миграции** - пошаговый план адаптации существующего кода (если нужно)
6. **Обновление документа** - изменение Конституции с header комментариями
7. **Обновление версии** - MAJOR для breaking changes, MINOR для дополнений
8. **Коммуникация** - уведомление команды, обновление `.kilocode/rules/` если нужно
9. **Миграция кода** - постепенная адаптация кода (не обязательно всё сразу)

**Формат header изменений** (обязательно в начале обновлённой Конституции):
```markdown
<!-- Version change: 2.0 → 3.0 -->
<!-- Date: 2025-11-14 -->
<!-- Author: Denis Egorov -->
<!-- Modified principles:
  - [SOLID] → Pragmatic Architecture Principles (упрощение, фокус на главном)
  - [Code Quality] → Modern Code Quality Metrics (Cognitive Complexity добавлено)
-->
<!-- Added sections:
  - [Modern Git Workflow] (Conventional Commits, trunk-based development)
  - [AI Agents Best Practices] (итеративность, безопасность)
-->
<!-- Removed sections:
  - [Deprecated Practices] (устаревшие требования к Python 3.9)
-->
<!-- Breaking changes:
  - Strict SOLID больше не требуется (D и S обязательны, остальные по необходимости)
  - Pylint score requirement снижен с 9.5 до 8.5 (фокус на критичные проблемы)
-->
<!-- Migration plan:
  - Existing code: No immediate changes required (works as is)
  - New code: Follow new pragmatic principles (simpler abstractions)
  - Refactoring: Gradually simplify over-engineered parts when touching them
-->
<!-- Related issues: #456, #789 -->
<!-- Follow-up tasks:
  - #500 Update .kilocode/rules/Ruls_prodject.md with new principles
  - #501 Simplify over-abstracted ShrinkageStrategy (when adding 5th strategy)
  - #502 Add Cognitive Complexity checks to CI/CD pipeline
-->
```

**Проверка соответствия Конституции** (для каждого PR):

**Автоматические проверки** (CI/CD, блокируют merge):
- ✅ Тесты проходят (Pytest)
- ✅ Покрытие >= 80% (pytest-cov)
- ✅ Типизация корректна (MyPy strict)
- ✅ Нет критичных уязвимостей (Bandit, pip-audit)
- ✅ Форматирование соблюдено (Black, Ruff)
- ✅ Pre-commit hooks прошли

**Ручные проверки reviewer** (чеклист):
- [ ] 🏗️ **Clean Architecture**: зависимости правильные, слои не нарушены
- [ ] 🧪 **TDD**: тесты были до кода, одобрены, все проходят
- [ ] 📊 **Coverage**: >= 80% общее, >= 95% domain/application
- [ ] 🎨 **UI**: Streamlit страницы через use cases, не напрямую к domain/infrastructure
- [ ] 🔐 **Security**: нет SQL injection, eval(), hardcoded secrets
- [ ] 📝 **Docs**: docstrings для public API, README/CHANGELOG обновлены
- [ ] 🎯 **Simplicity**: нет избыточных абстракций, код понятен
- [ ] 📏 **Complexity**: CC <= 10, вложенность <= 4, длина <= 50 строк
- [ ] 🚀 **Performance**: нет очевидных проблем (если есть - профилирование?)
- [ ] 🔧 **Pragmatism**: сложность обоснована, следует "Правилу трёх"

**Типовые причины REQUEST CHANGES**:
- ❌ Нарушена Clean Architecture (UI → Infrastructure напрямую)
- ❌ Нет тестов или TDD цикл не пройден (код до тестов)
- ❌ Добавлена сложная абстракция без обоснования (Strategy для 2 случаев)
- ❌ Покрытие упало ниже 80% (или ниже 95% для domain/application)
- ❌ MyPy strict ошибки или критичные Bandit warnings
- ❌ Docstrings отсутствуют для публичных функций
- ❌ CHANGELOG не обновлён (для feat/fix коммитов)

**Эскалация конфликтов**:
- 🤝 **Несогласие с Конституцией**: создать issue с предложением изменения + обоснование
- ⚖️ **Противоречие документов**: приоритет у Конституции, создать issue на синхронизацию
- ❓ **Неясность формулировки**: создать issue на уточнение, temporary решение с комментарием в PR
- 🚨 **Критический блокер**: эскалация к владельцу проекта через email + issue

**Контакты и ответственность**:
- **Проект**: shield_ai_v2 (Intelligent Inventory Management System)
- **Репозиторий**: [github.com/denisegorov2017-blip/shield_ai_v2](https://github.com/denisegorov2017-blip/shield_ai_v2)
- **Владелец**: Denis Egorov (окончательные решения по Конституции)
- **Email**: denis.egorov.2017@gmail.com
- **Ответственность**: Все участники (люди и ИИ-агенты) обязаны знать и соблюдать эту Конституцию

**Философия проекта** (современный подход):
- 🎯 **Прагматизм над догматизмом**: Правила служат проекту, не наоборот
- ⚡ **Скорость с качеством**: TDD + простота = быстрая надёжная разработка
- 🔄 **Эволюция кода**: Начинаем просто, усложняем когда нужно, рефакторим под тестами
- 🤖 **Люди + ИИ**: Автоматизация рутины, человек для критичных решений
- 📚 **Знания в коде**: Код самодокументируемый, комментарии для неочевидного
- 🧪 **Тесты как спецификация**: Тесты показывают как использовать код
- 🏗️ **Архитектура как инструмент**: Clean Architecture для гибкости, не для красоты

<!-- При нарушении Конституции PR отклоняется; Конституция - живой документ, развивается вместе с проектом -->

**Версия**: 2.1 | **Утверждено**: 2025-11-14 | **Последнее изменение**: 2025-11-14

<!--
Изменения в версии 2.0 (современный прагматичный подход):

ФИЛОСОФИЯ:
- Переход от строгого SOLID к прагматичным принципам (D+S обязательны, остальные по необходимости)
- Фокус на скорость + качество вместо "правильной архитектуры"
- Правило трёх для абстракций (3 дубля → абстракция)
- Эволюция кода: функция → класс → паттерн (только когда нужно)

ДОБАВЛЕНО:
- Современная типизация (Protocol, TypedDict, list[T] вместо List[T])
- Метрика Cognitive Complexity (важнее Cyclomatic для читаемости)
- Conventional Commits (автогенерация CHANGELOG)
- Оптимизированный CI/CD (fail fast, параллелизация, кэширование)
- Makefile как единая точка входа для команд
- Правила работы с ИИ-агентами (итеративность, безопасность, workflow)
- Streamlit как единая точка входа UI
- Детальные примеры docstrings и комментариев
- Философия проекта (прагматизм, эволюция, люди + ИИ)

ИЗМЕНЕНО:
- SOLID → Pragmatic Architecture Principles (упрощение)
- Pylint 9.5 → 8.5+ (фокус на критичные проблемы)
- Документация: автогенерация где возможно (CHANGELOG из commits, API docs из docstrings)
- Code Review: критичные vs важные vs необязательные проверки (приоритизация)

УДАЛЕНО:
- Требование реализовывать все принципы SOLID всегда
- Требование создавать абстракции заранее
- Требование 100% покрытия docstrings (public API достаточно)

СОХРАНЕНО:
- TDD как НЕОБСУЖДАЕМЫЙ принцип
- Clean Architecture направление зависимостей
- Минимум 80% покрытия тестами
- MyPy strict mode
- Безопасность (Bandit, pip-audit)

МИГРАЦИЯ:
- Существующий код работает как есть (no breaking changes)
- Новый код следует новым принципам (проще, быстрее)
- Постепенный рефакторинг переусложнённых частей
- ДОБАВЛЕНО: Принцип синхронности (весь код синхронный, SQLAlchemy Session, Streamlit sync, async только при обоснованной необходимости)
-->
