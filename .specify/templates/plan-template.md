# План реализации: \[ФУНКЦИЯ\]

**Ветка**: `[###-название-функции]` | **Дата**: \[ДАТА\] | **Спецификация**: \[ссылка\]
**Ввод**: Спецификация функции из `/specs/[###-название-функции]/spec.md`

**Примечание**: Этот шаблон заполняется командой `/speckit.plan`. См. `.specify/templates/commands/plan.md` для рабочего процесса выполнения.

## Резюме

\[Извлечение из спецификации функции: основное требование + технический подход из исследований\]

## Технический контекст

<!--
  ТРЕБУЕТСЯ ДЕЙСТВИЕ: Замените содержимое этого раздела техническими деталями
  для проекта. Здесь представлена структура в рекомендательных целях для
  процесса итерации.
-->

**Язык/Версия**: \[например, Python 3.11, Swift 5.9, Rust 1.75 или ТРЕБУЕТСЯ УТОЧНЕНИЕ\]\
**Основные зависимости**: \[например, FastAPI, UIKit, LLVM или ТРЕБУЕТСЯ УТОЧНЕНИЕ\]\
**Хранилище**: \[если применимо, например, PostgreSQL, CoreData, файлы или Н/Д\]\
**Тестирование**: \[например, pytest, XCTest, cargo test или ТРЕБУЕТСЯ УТОЧНЕНИЕ\]\
**Целевая платформа**: \[например, сервер Linux, iOS 15+, WASM или ТРЕБУЕТСЯ УТОЧНЕНИЕ\]
**Тип проекта**: \[одиночный/веб/мобильный - определяет структуру исходного кода\]\
**Цели производительности**: \[специфичные для области, например, 1000 запросов/с, 10000 строк/с, 60 кадров/с или ТРЕБУЕТСЯ УТОЧНЕНИЕ\]\
**Ограничения**: \[специфичные для области, например, \<200 мс p95, \<100 МБ памяти, возможность работы в автономном режиме или ТРЕБУЕТСЯ УТОЧНЕНИЕ\]\
**Масштаб/Объем**: \[специфичные для области, например, 10000 пользователей, 1М строк кода, 50 экранов или ТРЕБУЕТСЯ УТОЧНЕНИЕ\]

## Проверка конституции

*ПРОХОД: Должен пройти до исследования фазы 0. Повторная проверка после проектирования фазы 1.*

\[Ворота определяются на основе файла конституции\]

## Структура проекта

### Документация (эта функция)

```text
specs/[###-функция]/
├── plan.md              # Этот файл (вывод команды /speckit.plan)
├── research.md          # Вывод фазы 0 (команда /speckit.plan)
├── data-model.md        # Вывод фазы 1 (команда /speckit.plan)
├── quickstart.md        # Вывод фазы 1 (команда /speckit.plan)
├── contracts/           # Вывод фазы 1 (команда /speckit.plan)
└── tasks.md             # Вывод фазы 2 (команда /speckit.tasks - НЕ создается командой /speckit.plan)
```

### Исходный код (корень репозитория)

<!--
  ТРЕБУЕТСЯ ДЕЙСТВИЕ: Замените дерево-заполнитель ниже конкретной структурой
  для этой функции. Удалите неиспользуемые варианты и расширьте выбранную структуру
  реальными путями (например, apps/admin, packages/что-то). Доставленный план не должен
  включать метки вариантов.
-->

```text
# [УДАЛИТЬ, ЕСЛИ НЕ ИСПОЛЬЗУЕТСЯ] Вариант 1: Один проект (ПО УМОЛЧАНИЮ)
src/
├── models/
├── services/
├── cli/
└── lib/

tests/
├── contract/
├── integration/
└── unit/

# [УДАЛИТЬ, ЕСЛИ НЕ ИСПОЛЬЗУЕТСЯ] Вариант 2: Веб-приложение (когда обнаружены "frontend" + "backend")
backend/
├── src/
│   ├── models/
│   ├── services/
│   └── api/
└── tests/

frontend/
├── src/
│   ├── components/
│   ├── pages/
│   └── services/
└── tests/

# [УДАЛИТЬ, ЕСЛИ НЕ ИСПОЛЬЗУЕТСЯ] Вариант 3: Мобильное + API (когда обнаружено "iOS/Android")
api/
└── [такой же, как backend выше]

ios/ или android/
└── [специфичная для платформы структура: модули функций, UI-потоки, платформенные тесты]
```

**Решение по структуре**: \[Документируйте выбранную структуру и ссылайтесь на реальные
каталоги, указанные выше\]

## Отслеживание сложности

> **Заполнить ТОЛЬКО если проверка конституции имеет нарушения, которые должны быть оправданы**

| Нарушение | Почему необходимо | Более простая альтернатива отклонена, потому что |
|-----------|------------|-------------------------------------|
| \[например, 4-й проект\] | \[текущая необходимость\] | \[почему 3 проекта недостаточно\] |
| \[например, паттерн Repository\] | \[конкретная проблема\] | \[почему прямой доступ к БД недостаточен\] |
