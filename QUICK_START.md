# 🚀 Быстрый старт Shield AI v2.0

## Установка (30 секунд)

### 1. Клонирование репозитория

```bash
git clone https://github.com/denisegorov2017-blip/shield_ai_v2.git
cd shield_ai_v2
```

### 2. Установка зависимостей

**Через Poetry (рекомендуется):**

```bash
# Установка Poetry (если не установлен)
curl -sSL https://install.python-poetry.org | python3 -

# Установка зависимостей
poetry install
```

**Или через pip:**

```bash
pip install -r requirements.txt
```

### 3. Инициализация базы данных

```bash
# С Poetry
poetry run python -c "from shield_ai.infrastructure.database import init_db; init_db()"

# Или через Makefile
make init-db
```

### 4. Запуск приложения

```bash
# С Poetry
poetry run streamlit run main.py

# Или через Makefile
make run

# Откроется в браузере: http://localhost:8501
```

______________________________________________________________________

## Основные команды

### Разработка

```bash
make help       # Показать все команды
make format     # Форматирование кода (black + isort)
make lint       # Линтинг (ruff)
make pylint     # Проверка стиля кода (pylint)
make mypy       # Проверка типов (mypy)
make pyright    # Проверка типов (pyright)
make test       # Запуск тестов
make ci         # Полный CI пайплайн
```

______________________________________________________________________

## Контроль качества кода

В проекте используется несколько инструментов для обеспечения качества кода:

### 1. Pyright

Pyright - это мощный статический анализатор типов для Python, разработанный Microsoft. Он помогает находить ошибки типизации и обеспечивает более высокую надежность кода.

**Установка:**
Pyright уже установлен как зависимость проекта (см. `pyproject.toml`).

**Настройка:**
Pyright настроен через файл `pyrightconfig.json`, который включает:

- Проверку файлов в директориях `src`, `tests`, `scripts`, и `src/shield_ai/presentation/ui/pages`
- Исключение служебных директорий (`.git`, `__pycache__`, `.venv` и др.)
- Установку версии Python 3.12
- Добавление директории `src` в пути поиска модулей

**Запуск:**

```bash
# Через Makefile
make pyright

# Напрямую через Poetry
poetry run pyright

# Или с указанием конкретной директории
poetry run pyright src/
```

### 2. Pylint

Pylint - это инструмент статического анализа кода, который проверяет соответствие кода стандартам Python и выявляет потенциальные ошибки.

**Установка:**
Pylint уже установлен как зависимость проекта (см. `pyproject.toml`).

**Настройка:**
Pylint настроен через файл `.pylintrc`, который включает:

- Порог оценки 8.0 (ниже этого значения сборка считается неудачной)
- Игнорирование служебных директорий (`.git`, `.venv`, `__pycache__` и др.)
- Отключение определенных предупреждений (C0103, C0114, C0115, C0116, C0301, R0903, R0913, W0613)
- Максимальная длина строки 88 символов (согласовано с black и ruff)
- Ограничения на количество аргументов, локальных переменных, возвращаемых значений и ветвлений в функциях

**Запуск:**

```bash
# Через Makefile
make pylint

# Напрямую через Poetry
poetry run pylint src/ tests/

# Или с указанием конкретной директории
poetry run pylint src/
```

**Pre-commit хуки:**
Pylint также интегрирован в систему pre-commit хуков, что позволяет автоматически проверять качество кода при каждом коммите. Для настройки pre-commit хуков выполните:

```bash
# Установка pre-commit хуков
poetry run pre-commit install

# Ручной запуск проверки всех файлов
poetry run pre-commit run --all-files
```

______________________________________________________________________

## Работа с системой

### 1. Загрузка данных

1. Перейди в раздел **"📁 Парсинг"**
1. Загрузи Excel-файл с движением товаров
1. Нажми "🚀 ЗАГРУЗИТЬ И РАСПАРСИТЬ"

### 2. Калибровка коэффициентов

1. Перейди в раздел **"⚙️ Калибровка"**
1. Нажми "🚀 ЗАПУСТИТЬ КАЛИБРОВКУ"
1. Система рассчитает коэффициенты для каждого товара

**Используется**: ПОРЦИОННАЯ модель (99.9% точность)

### 3. Прогнозирование усушки

1. Перейди в раздел **"🔮 Прогноз"**

1. Нажми "🔮 РАССЧИТАТЬ ПРОГНОЗ"

1. Получи прогноз усушки для всех активных партий

**Используется**: ВЗВЕШЕННАЯ модель (99.5% точность + высокая производительность)

______________________________________________________________________

## Настройка

### Создайте `.env` файл

```bash
cp .env.example .env
```

Отредактируйте параметры по необходимости.

______________________________________________________________________

## Троублшутинг

### Проблема: Не запускается Streamlit

```bash
# Убедитесь, что Streamlit установлен
poetry run streamlit --version

# Или установите вручную
poetry add streamlit
```

### Проблема: Ошибки импорта

```bash
# Добавьте src в PYTHONPATH
export PYTHONPATH="${PYTHONPATH}:${PWD}/src"
```

______________________________________________________________________

## Документация

- [📋 Модели усушки](docs/Modeli_usushki.md) - Научная документация
- [🏭 Архитектура](docs/ARCHITECTURE.md) - Описание архитектуры
- [📖 README](README.md) - Общая информация

______________________________________________________________________

## **Готово к работе!** 🛡️🚀

______________________________________________________________________

## Использование фреймворка [`spec-kit`](https://github.com/github/spec-kit)

`spec-kit` – это фреймворк, который помогает в разработке, управляемой спецификациями (Spec-Driven Development), автоматизируя создание документации, планов, задач и контекста для AI-агентов. Он обеспечивает структурированный подход к разработке функций, гарантируя согласованность и полноту документации.

### Краткое описание инструментов `spec-kit`:

- **`create-new-feature.sh`**: Создает новую ветку функции и соответствующую директорию спецификаций с базовым файлом `spec.md`. Используется для начала работы над новой функцией.
- **`setup-plan.sh`**: Создает или обновляет файл `plan.md` для текущей функции, используя шаблон. Помогает детализировать технический план реализации.
- **`update-agent-context.sh`**: Обновляет файлы контекста для AI-агентов (например, Claude, Gemini) информацией из `plan.md`. Это позволяет агентам лучше понимать проект и текущую задачу.
- **`check-prerequisites.sh`**: Проверяет наличие необходимых файлов (например, `plan.md`, `tasks.md`) перед выполнением следующих шагов в рабочем процессе.
- **`common.sh`**: Набор общих функций, используемых другими скриптами `spec-kit`, таких как определение корня репозитория и текущей ветки.

### Обновление `spec-kit`

Для обновления фреймворка `spec-kit` обычно требуется получить последние изменения из его репозитория. Если `spec-kit` интегрирован как подмодуль Git или как часть вашего репозитория, вы можете выполнить следующие шаги:

```bash
# Перейдите в директорию spec-kit, если это подмодуль
cd .specify # Или путь к вашей установке spec-kit

# Получите последние изменения

git pull origin main # Или соответствующая ветка

# Вернитесь в корневую директорию проекта
cd -
```

Если `spec-kit` установлен другим способом, обратитесь к его официальной документации для получения инструкций по обновлению.

______________________________________________________________________

## Использование `spec-kit` с KiloCode

KiloCode, как оркестратор рабочих процессов, использует инструменты `spec-kit` для эффективного управления задачами. Вы можете взаимодействовать с KiloCode, используя команды, которые активируют скрипты `spec-kit`.

### Примеры использования:

- **`create-new-feature.sh` - Создание новой функции:**

  - **Что это делает:** Инициализирует новую ветку Git и создает базовую структуру для новой функции в директории `.specify/features/`.

  - **Ваш запрос к KiloCode:** "Создай новую функцию для калибровки моделей усушки, назови ее 'calibrate-shrinkage-models'."

  - **Действие KiloCode:**

    ```xml

    <new_task>
    <mode>code</mode>
    <message>
    Используй скрипт create-new-feature.sh с описанием 'калибровка моделей усушки' и коротким именем 'calibrate-shrinkage-models'.
    </message>
    </new_task>
    ```

  - **Ответ KiloCode (симуляция):**

    ```xml
    <execute_command>
    <command>scripts/create-new-feature.sh "калибровка моделей усушки" "calibrate-shrinkage-models"</command>
    </execute_command>
    ```

    *Затем KiloCode сообщит о результате, например: "Создана новая ветка `feature/001-calibrate-shrinkage-models` и директория `.specify/features/001-calibrate-shrinkage-models/` с файлом `spec.md`."*

- **`setup-plan.sh` - Разработка плана реализации:**

  - **Что это делает:** Создает или обновляет файл `plan.md` в директории текущей функции для детализации технического плана.
  - **Ваш запрос к KiloCode:** "Разработай план реализации для функции 'calibrate-shrinkage-models'."
  - **Действие KiloCode:**
    ```xml
    <execute_command>
    <command>scripts/setup-plan.sh</command>
    </execute_command>

    ```
    *KiloCode выполнит этот скрипт, находясь в ветке `feature/001-calibrate-shrinkage-models`, чтобы сгенерировать `.specify/features/001-calibrate-shrinkage-models/plan.md`.*
  - **Ответ KiloCode (симуляция):**
    *После выполнения KiloCode предложит вам отредактировать созданный план: "Файл `plan.md` создан. Пожалуйста, просмотрите и дополните его деталями реализации."*

- **`update-agent-context.sh` - Обновление контекста для AI:**

  - **Что это делает:** Автоматически обновляет файлы контекста (например, `CLAUDE.md`, `GEMINI.md`) на основе `plan.md` и `spec.md`.
  - **Как это работает:** KiloCode запускает этот скрипт в фоновом режиме после каждого изменения в планах или спецификациях. Вам не нужно запрашивать это напрямую.
  - **Пример:** После того как вы обновили `plan.md`, KiloCode выполнит:
    ```xml
    <execute_command>
    <command>scripts/update-agent-context.sh</command>
    </execute_command>
    ```
  - **Ответ KiloCode (симуляция):**
    *"Контекст для AI-агентов обновлен на основе последних изменений в `plan.md`."*

- **`check-prerequisites.sh` - Проверка готовности:**

  - **Что это делает:** Проверяет, что все необходимые файлы (`spec.md`, `plan.md`) на месте перед выполнением критически важных задач.
  - **Как это работает:** KiloCode автоматически запускает этот скрипт перед генерацией кода, тестов или созданием списка задач.
  - **Пример:** Перед тем как сгенерировать список задач, KiloCode выполнит:
    ```xml
    <execute_command>
    <command>scripts/check-prerequisites.sh "tasks"</command>
    </execute_command>
    ```
  - **Ответ KiloCode (симуляция):**
    *Если проверка пройдена: "Все необходимые файлы на месте. Готов к следующему шагу."*
    *Если проверка не пройдена: "Отсутствует `plan.md`. Пожалуйста, создайте план перед продолжением."*

### Общий рабочий процесс с KiloCode и `spec-kit`

1. **Инициирование задачи**: Вы запрашиваете у KiloCode новую функцию или задачу (например, "Создай новую функцию для \[описание\]").

1. **KiloCode делегирует**: KiloCode анализирует ваш запрос и использует соответствующий скрипт `spec-kit` (например, `create-new-feature.sh`, `setup-plan.sh`, `update-agent-context.sh` или `check-prerequisites.sh`) для выполнения задачи.

1. **Итеративная разработка**: В процессе работы KiloCode будет направлять вас, предлагая следующие шаги или автоматически выполняя действия с помощью `spec-kit` для создания, обновления и проверки документации и кода.

1. **Отчетность**: KiloCode предоставляет подробные отчеты о ходе выполнения, результатах использования инструментов `spec-kit` и любых обнаруженных проблемах, обеспечивая прозрачность рабочего процесса.

______________________________________________________________________
